# ============================================================
# OpenAI (Recommended — powers the interview + blueprint)
# ============================================================
# Get your key at: https://platform.openai.com/api-keys
#
# Used for:
#   - gpt-4o-mini  → per-turn interview acknowledgments (cheap, ~$0.001/run)
#   - gpt-4o       → final persona interpretation + blueprint arc design
#
# Without this key: the app falls back to keyword matching.
# Fully functional, just less conversational and nuanced.
#
OPENAI_API_KEY=sk-...
